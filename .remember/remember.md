# Handoff

## State
Shipped E1 (Git Object Model Visualizer) via Lovable burn: live at https://git-inkwell.lovable.app, source at github.com/skchaudr/git-object-explorer-v1, local clone at `pages/011-git-object-explorer/` (gitignored, managed via own .git). Tracked marker: `pages/011-git-object-explorer.md`. Outer repo has new `LOVABLE_BURN_BACKLOG.md` at root with 6 fluency series, house style, no-drills principle. All pushed to main (last commit `2c75294`).

## Next
1. Sab may install bun (`curl -fsSL https://bun.sh/install | bash`) so he can run `bun dev` on `pages/011-git-object-explorer/` locally. Alternative: `npm install && npm run dev` with existing Node 25.9.0.
2. Verify whether `bun run build` produces purely static output (Python http.server on pi-small) or includes a server runtime (TanStack Start + Wrangler suggests edge SSR — needs checking).
3. Backlog has 17 entries queued. Next likely picks: F3 RAG Pipeline Explorer, D2 Zed AI Workflow Explorer, E2 Reflog & Recovery Lab.

## Context
- Sab gave full commit+push permission on main for this workflow — don't re-ask each time.
- Drills are unproven format for Sab in HTML; don't default to them — web apps should be explorers/visualizers/sandboxes/references (memory: `feedback_no_drills_in_web_apps.md`).
- New memories saved this session: `feedback_write_file_dont_chat.md`, `feedback_no_drills_in_web_apps.md`, `feedback_no_defensive_signoffs.md`.
- Lovable account hopping happened mid-session (old repo `git-object-explorer` → new `git-object-explorer-v1`); URLs are already patched.
- Page 010 (`obsidian-neovim-fluency/`) was untracked pre-session and remains untracked — unrelated to today's work.
